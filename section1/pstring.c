#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(int argc, string argv[])
{
    string name = "Robbie";

    for (int i = 0, n = strlen(name); i < n; i++)
    {
	printf("%c\n", name[i]);
    }
}
